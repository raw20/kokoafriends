import { makeVar } from "@apollo/client";

export const isOpenDeleteReviewDialogVar = makeVar(false);
export const isOpenUpdateReviewDialogVar = makeVar(false);
export const isOpenDeleteCartDialogVar = makeVar(false);
